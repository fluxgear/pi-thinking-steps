# Agent Notes

## Thinking steps preferences

Mode precedence is session -> project -> global -> summary.

Use `/thinking-steps project clear` to remove the project default.
Use `/thinking-steps global clear` to remove the global default.

These notes document the persistence phrases covered by the repository metadata contract test.
